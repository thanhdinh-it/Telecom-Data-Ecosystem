# Python dependencies — Telecom Data Ecosystem
# Cài đặt: pip install -r requirements.txt

# ── Data Generation ──────────────────────────────────────────
faker==22.2.0
pandas==2.1.4

# ── Kafka ────────────────────────────────────────────────────
kafka-python==2.0.2

# ── PySpark (local dev) ──────────────────────────────────────
# Trong Docker thường dùng bitnami/spark image có sẵn pyspark
pyspark==3.4.2

# ── dbt ──────────────────────────────────────────────────────
dbt-postgres==1.7.4
dbt-core==1.7.4

# ── Testing ──────────────────────────────────────────────────
pytest==7.4.4
pytest-mock==3.12.0

# ── Database ─────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── Airflow (local dev — prod dùng official Docker image) ───
# Cài Airflow qua pip chỉ cho local dev/testing
# apache-airflow==2.8.0  # Uncomment nếu cần local Airflow

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.0
